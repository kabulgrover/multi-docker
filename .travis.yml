sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t kabulgrover/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kabulgrover/react-test npm test

after_success:
  - docker build -t kabulgrover/multi-client ./client
  - docker build -t kabulgrover/multi-nginx ./nginx
  - docker build -t kabulgrover/multi-server ./server
  - docker build -t kabulgrover/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push kabulgrover/multi-client
  - docker push kabulgrover/multi-nginx
  - docker push kabulgrover/multi-server
  - docker push kabulgrover/multi-worker
